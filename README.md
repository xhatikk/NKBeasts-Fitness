# NKBEASTS Fitness — checked build

- `index.html` is at the deployment root.
- `functions/api/news.js` and `functions/api/config.js` handle the Cloudflare Pages API routes.
- AdSense publisher ID matches `ads.txt`.
- Upload the extracted files to the repository root; do not upload the ZIP as a single website file.
